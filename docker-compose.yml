services:
  postgres:
    image: postgres:15.3
    restart: always
    container_name: postgres
    environment:
      POSTGRES_USER: seconde
      POSTGRES_PASSWORD: seconde.app
      POSTGRES_DB: url_shortener
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

